{
    admin off
    auto_https off
}

:{$PORT:8080} {
    encode zstd gzip

    handle /api/* {
        reverse_proxy 127.0.0.1:8000
    }

    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
